import { ComingSoon } from '@/components/app/coming-soon'

export default function UsersPage() {
  return (
    <ComingSoon
      module="Users"
      description="Invite, manage, and deactivate the firm's lawyers and clerks. Each access tier is enforced via row-level security in the database."
    />
  )
}
