import { ComingSoon } from '@/components/app/coming-soon'

export default function CalendarPage() {
  return (
    <ComingSoon
      module="Calendar"
      description="Hearings, mentions, case management dates, and internal deadlines — across every matter, every court, every counsel."
    />
  )
}
