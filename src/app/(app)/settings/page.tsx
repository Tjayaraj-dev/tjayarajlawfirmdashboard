import { ComingSoon } from '@/components/app/coming-soon'

export default function SettingsPage() {
  return (
    <ComingSoon
      module="Settings"
      description="Firm profile, Bar Council number, branding, retention policy, notification preferences, and backup configuration."
    />
  )
}
