import { ComingSoon } from '@/components/app/coming-soon'

export default function DocumentsPage() {
  return (
    <ComingSoon
      module="Documents"
      description="The document vault — pleadings, affidavits, correspondence, KYC, evidence, and invoices. Versioned, soft-deleted, signed-URL retrievable."
    />
  )
}
