import { ComingSoon } from '@/components/app/coming-soon'

export default function MattersPage() {
  return (
    <ComingSoon
      module="Matters"
      description="The matter ledger — file references, parties, courts, hearings, and document trails. Schema designed, UI lands once the firm's onboarding answers return."
    />
  )
}
